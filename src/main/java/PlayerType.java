package gamelogic;

public enum PlayerType {
    /**
     * This is for setting the boundaries for the paddle.
     * (So the paddle does not go into the other players side of the board).
     */
    PLAYER1, PLAYER2
}
